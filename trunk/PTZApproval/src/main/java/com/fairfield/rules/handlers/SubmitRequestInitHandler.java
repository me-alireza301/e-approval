package com.fairfield.rules.handlers;

import java.util.HashMap;
import java.util.Map;

import org.jbpm.graph.def.ActionHandler;
import org.jbpm.graph.exe.ExecutionContext;

public class SubmitRequestInitHandler implements ActionHandler {

	@Override
	public void execute(ExecutionContext executionContext) throws Exception {
		/*Map<String,Object> formElements = new HashMap<String, Object>();
		formElements.put("userId","");
		formElements.put("userName","" );
		formElements.put("processId","");
		formElements.put("remark","");
		executionContext.getContextInstance().addVariables(formElements);*/
	}
}
